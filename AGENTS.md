# تعليمات مشروع tele-treak

- لا تغيّر منطق الستريك أو الإحياء أو الحماية أثناء تعديل الستوري إلا إذا كان ذلك مطلوبًا صراحة.
- كل حالة مستمرة للمستخدمين تحفظ في قاعدة البيانات؛ الذاكرة تستخدم للكاش المؤقت فقط.
- فيديو الستوري صامت افتراضيًا، والأغنية مصدرها ملف يرفعه المستخدم، وليس خدمة خارجية.
- أي زر جديد في الستوري يجب أن يملك مسارًا مكافئًا في الواجهة الغنية والواجهة الاحتياطية عند الحاجة.
- اختبر مسارات النشر مع الوسائط وبدونها، وانتهاؤها، وصلاحيات الطرفين.
- لا تضف أسرارًا أو Cookies أو ملفات Telegram المرفوعة إلى Git.

## توزيع الملفات

- `app/adventures/`: المهام، القواعد، وصفحات المغامرة.
- `app/adventures/achievements.py`: تعريف الإنجازات وقواعد فتحها.
- `app/services/adventure_service.py`: تنسيق التدفق فقط؛ الرندر في `app/story/renderer.py`.
- `app/database/`: الجداول والمستودعات والترقيات التلقائية.
- `app/handlers/`: استقبال أوامر Telegram والـ callbacks، بدون منطق قاعدة بيانات طويل.
- `tests/`: اختبارات القواعد والمستودعات وتدفق الستوري.
